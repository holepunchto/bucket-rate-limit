{
  "name": "bucket-rate-limit",
  "version": "1.0.0",
  "description": "A lightweight, bucket-based rate limiter for JavaScript that controls request frequency with minimal overhead.",
  "homepage": "https://github.com/holepunchto/bucket-rate-limit#readme",
  "bugs": {
    "url": "https://github.com/holepunchto/bucket-rate-limit/issues"
  },
  "files": [
    "index.js"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/holepunchto/bucket-rate-limit.git"
  },
  "license": "Apache-2.0",
  "author": "Holepunch Inc",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "test": "npm run lint && brittle test/*"
  },
  "devDependencies": {
    "brittle": "^3.19.1",
    "prettier": "^3.6.2",
    "prettier-config-holepunch": "^2.0.0"
  },
  "dependencies": {
    "signal-promise": "^1.0.3"
  }
}
